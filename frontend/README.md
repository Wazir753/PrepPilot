# PrepPilot Frontend

React 16 SPA for the PrepPilot AI Interview Simulator.

## Setup

```bash
cp .env.example .env
npm install --legacy-peer-deps
npm start
```

## Build

```bash
npm run build
```

## Docker

```bash
docker build -t preppilot-frontend .
```

Served via nginx on port 80.
